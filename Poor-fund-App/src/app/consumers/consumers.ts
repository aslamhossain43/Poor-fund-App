export class Consumers {
    id: string;
    piCode: string;
    name: string;
    country: string;
    zela: string;
    upozela: string;
    union: string;
    work: string;
    contact: number;
    bkash: number;
    apiCode: string;
    prCode: string;
    createdDate: string;
    lastModifiedDate: string;
    // FOR AUTH UID
    uid: string;
    // FOR FILE RESET ONLY
    selectedPiFile: File;
    selectedApiFile: File;
}
